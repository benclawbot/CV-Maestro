# Keep this file local. Do not prefix the API key with VITE_.
MINIMAX_API_KEY=your_minimax_api_key

# MiniMax M3 is not currently listed in MiniMax's public model catalogue.
# Set this to MiniMax-M3 when that model is available to your account.
VITE_MINIMAX_MODEL=MiniMax-M2.7
